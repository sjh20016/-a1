# V3.4.3 文案体验基线报告

日期：2026-07-14

分支：`v343-experience-baseline`

产品版本基线：`3.4.2`

## 本次提升

- 新增 `concise-v1`、`balanced-v1`、`immersive-v1` 三档版本化文案策略，默认使用沉浸档。
- `NarrativeQualityPlan` 升级到 1.1，按真人数、事实数、互动、代价和复合行动计算目标篇幅。
- 新增 `NarrativePacket 1.0`，把公开事实、互动、场景、白名单、段落职责和目标长度作为最小写作包发送给 AI。
- 提示词明确要求围绕理想字数写作，并使用“动作—即时反应—具体后果”的场面链。
- 发布闸门使用档位化字符底线；最低发布线和理想体验线分离。
- 新增 `narrativeMetrics 1.0`，记录长度、段落、句长变化、对白比例、段落平衡、体验分数和等级。
- 最近 20 章指标进入 `narrativeQualityHistory`，为后续真实模型长期压测提供趋势数据。
- 浏览器、单人、本地房间和云端房间统一透传文案档位。
- 服务端默认 `AI_MAX_TOKENS` 从 2600 提高到 4096，降低完整章节因输出预算不足而截断的风险。

## 30 轮确定性基线

命令：

```bash
npm run simulate -- --seed V343-BASELINE --turns 30 --quiet
```

结果：

| 指标 | 结果 |
| --- | ---: |
| 完成回合 | 30 / 30 |
| 真人行动 | 90 |
| 多人互动 | 6 |
| 叙事失败 | 0 |
| 平均章节长度 | 1540.7 字 |
| P50 章节长度 | 1538 字 |
| P90 章节长度 | 1659 字 |
| 平均段落数 | 5.8 |
| 平均体验分 | 100 |
| 目标篇幅命中 | 30 / 30 |

该基线使用本地确定性 Provider，不代表真实模型文学评分；它验证的是规划、长度预算、提交链、指标采集和长线运行不会互相破坏。真实 DeepSeek 压测可以在此基础上比较行动覆盖率、自动返工率、平均篇幅和体验告警。

## 回归结果

- `npm run check:syntax`：通过。
- `npm test`：全套通过。
- V3.4.2 三真人十轮夹具：30/30 行动、12 项互动、10/10 章节发布。
- V3.4.3 文案体验测试：16/16 通过。
- `npm audit --omit=dev`：0 漏洞。

## 后续建议

1. 用测试专用 DeepSeek Provider 运行同种子 10 至 30 轮，记录 `narrativeMetrics` 与语义返工结果。
2. 将软质量告警按错误类型注入一次可选文风返工，不与事实返工共用预算。
3. 在每房间 `StoryEngine` 实例化之前保留本报告和模拟输出，作为重构行为基线。
4. 后续引入结构化 `NarrativePlan` 时复用 `NarrativePacket`，避免再次扩大完整 brief 上下文。
